spring:
  profiles:
    active: local # 기본 환경 선택

# local 환경
---
spring:
  profiles: local
  datasource:
    master:
      driver-class-name: "com.mysql.jdbc.Driver"
      url: "jdbc:mysql://127.0.0.1:8080"

