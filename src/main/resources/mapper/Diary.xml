<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hirim.sulgijang.repositories.DiaryRepository">

    <insert id="insertDiary" keyProperty="diaryId" useGeneratedKeys="true">
        INSERT INTO diary(diary_name, meeting_date, loser, created_by, created_at)
        VALUES (#{diaryName}, #{meetingDate}, #{loser}, #{createdBy}, SYSDATE())
    </insert>

    <insert id="insertDiaryContent" keyProperty="diaryContentId" useGeneratedKeys="true">
        INSERT INTO diary_content(diary_id, place_id, summary, snack, depth, created_by, created_at)
        VALUES (#{diaryId}, #{placeId}, #{summary}, #{snack}, #{depth}, #{createdBy}, SYSDATE())
    </insert>

    <update id="updateDiary">
        UPDATE diary
        SET diary_name = #{diaryName},
            loser = #{loser},
            meeting_date = #{meetingDate},
            updated_at = SYSDATE(),
            updated_by = #{updatedBy}
        WHERE diary_id = #{diaryId}
    </update>

    <update id ="updateDiaryContent">
        UPDATE diary_content
        SET updated_by    = #{updatedBy},
            updated_at    = SYSDATE(),
            place_name = #{placeName},
            summary = #{summary},
            snack = #{snack},
            depth = #{depth}
        WHERE diary_content_id = #{diaryContentId}
    </update>

    <delete id="deleteDiary">
        DELETE FROM diary
        WHERE diary_id = #{diaryId}
    </delete>

    <delete id="deleteDiaryContent">
        DELETE FROM diary_content
        WHERE diary_id = #{diaryId}
    </delete>

    <select id="selectDiaryList" resultType="Diary">
        SELECT *
        FROM diary
        WHERE 1 = 1
        <if test="diaryId > 0">
            AND diary_id = #{diaryId}
        </if>
        <if test="partyId > 0">
            AND party_id = #{partyId}
        </if>
    </select>

    <select id="selectDiary" resultType="Diary">
        SELECT *
        FROM diary
        WHERE 1 = 1
        <if test="diaryId > 0">
            AND diary_id = #{diaryId}
        </if>
        <if test="diaryName != null">
            AND diary_name = #{diaryName}
        </if>
    </select>

    <select id="selectDiaryContentList" resultType="DiaryContent">
        SELECT *
        FROM diary_content
        WHERE diary_id = #{diaryId}
    </select>

</mapper>